# 工程规则（永久生效）

以下规则为所有项目通用的最高准则，任何会话中都必须遵守。

## 一、核心设计原则（所有项目通用最高准则）

### 契约优先，兼容至上

- 对外接口、数据模型、公共 API 是系统的基石。严禁随意修改字段含义、删除已有字段、变更返回结构、修改入参语义。
- 确需变更时，必须先做向下兼容的扩展式改造；破坏性变更必须经过版本演进、过渡期兼容、废弃声明三个阶段。
- 启动方式、配置格式、命令行参数的对外行为必须保持兼容，禁止无预警变更用户使用习惯。

### 开闭原则：扩展优于修改

- 新增能力优先通过「新增实现 + 注册配置」完成，禁止侵入修改稳定的核心模块。
- 同类差异逻辑通过抽象接口 + 多实现承载，禁止在核心流程中堆砌 if/else 分支、硬编码特殊判断。
- 稳定模块只修 Bug、不新增业务逻辑；新业务全部在扩展层实现。

### 单一职责：一个单元只做一件事

- 函数、类、模块、服务都必须遵循单一职责。一个函数只完成一个明确的功能，一个模块只负责一个领域的能力。
- 出现 “既做 A 又做 B” 的单元时，必须拆分，禁止合并无关逻辑。

### 最小改动原则

- 实现一个需求只修改最少的文件、最少的代码行。不相关的重构、格式调整、变量重命名必须单独提交，不得混在功能提交中。
- 禁止 “顺手优化”：修复 A 问题时，不得顺带重构 B 模块、调整 C 风格。一次提交只解决一个明确问题。

### 可维护性 > 炫技式简洁 > 微性能优化

- 优先写清晰、可读、普通人能快速看懂的代码。拒绝为了少写几行使用晦涩语法、黑魔法技巧。
- 性能优化必须有实测数据支撑，禁止无依据的臆想式优化。核心路径优化必须附带基准测试对比。
- 禁止为了 “代码优雅” 牺牲可读性和可调试性。

## 二、架构与分层通用准则（从根源防止架构腐化）

### 分层边界不可突破

- 严格遵守领域层、适配器层、业务服务层、传输层、展示层的分层职责，禁止跨层泄露逻辑。
- 上层可以调用下层，下层不能反向依赖上层；核心领域层不能依赖任何外部基础设施。
- 业务逻辑不能写在展示层，外部系统细节不能泄露到核心领域层。

### 依赖方向必须单向

- 依赖必须从外向内、从具体到抽象，禁止循环依赖、反向依赖。
- 核心模块依赖抽象接口，不依赖具体实现；具体实现可以依赖核心抽象。
- 新增依赖前必须校验方向，出现循环依赖必须重构解耦。

### 公共能力必须下沉复用

- HTTP 请求、重试策略、超时控制、文件操作、加解密、日志格式、错误处理、参数校验等通用能力，必须统一收口到公共基础库。
- 发现同类逻辑出现第二次时，必须抽取公共抽象，禁止放任复制粘贴。
- 新增业务模块必须复用现有公共能力，禁止重复造轮子。

### 高内聚，低耦合

- 相关联的逻辑、数据、行为要聚合在同一个模块内；不相关的能力必须隔离。
- 模块间通过稳定的接口通信，不暴露内部实现细节，不直接读写对方内部状态。
- 修改一个模块时，应尽可能少地影响其他模块。

## 三、通用代码质量规范（全语言适用）

### 命名规范

- 变量、函数、类、模块命名必须见名知意，禁止单字母、拼音、模糊缩写。
- 布尔值以 is/has/can/should 开头；函数以动词开头；集合类型加复数后缀。
- 同一概念在全项目中命名必须统一，禁止同一事物出现多种命名。
- 命名风格跟随项目既有规范，保持与现有代码一致。

### 函数与模块粒度

- 单个函数职责单一，长度原则上不超过 80 行；超过必须拆分。
- 深层嵌套（超过 3 层 if/for 嵌套）必须通过提前返回、卫语句、抽取函数简化。
- 一个文件只承载一类职责，不相关的函数不得堆放在同一文件中。

### 错误处理标准化

- 禁止静默吞掉错误：禁止空 catch、忽略错误返回值、滥用 unwrap/异常捕获。
- 所有错误必须携带上下文信息：哪个环节、哪个资源、具体原因，便于定位排查。
- 业务错误与系统错误分离，定义统一错误码；上层只依赖错误码，不依赖错误文案。
- 异常场景必须有明确的降级、重试、恢复策略，不能直接崩溃。

### 配置与常量管理

- 禁止业务硬编码：端口、地址、阈值、超时时间、开关、特征值所有可变配置，必须走配置文件或环境变量。
- 魔法数字、魔法字符串必须提取为具名常量，并注释含义。
- 新增配置必须有默认值，默认值适配绝大多数场景，保证零配置可运行。

### 依赖治理

- 新增第三方依赖必须有充分理由，禁止为一个小功能引入重型库。
- 依赖版本必须锁定，遵循语义化版本规范，禁止使用通配符版本。
- 严格区分生产依赖与开发依赖，禁止混淆。
- 定期治理冗余依赖，未使用的依赖必须及时移除。

## 四、主流语言专项企业级规范

### TypeScript / 前端项目

- 所有组件、函数、Hook 必须有完整类型定义，禁止滥用 any，类型断言必须附带合理性说明。
- 业务状态与 UI 状态分离，通用逻辑抽取为自定义 Hook，禁止在组件内堆砌大量业务逻辑。
- API 请求统一收口到请求层，禁止组件内直接编写请求逻辑。
- 组件粒度合理，禁止上千行的巨型页面组件，按职责拆分为容器组件、展示组件、业务子组件。
- 副作用必须有清理逻辑，禁止内存泄漏、重复订阅。

### Python 项目

- 遵循 PEP 8 规范，核心函数必须有完整类型注解。
- 异常捕获必须精确到异常类型，禁止裸 except 吞掉所有异常。
- 全局状态最小化，优先使用纯函数，减少模块级可变变量。
- 核心逻辑必须编写文档字符串，说明入参、返回值、异常场景。

### Rust 项目

- 遵循官方 API 指南，合理使用所有权与借用，禁止大量无意义 clone() 敷衍编译问题。
- 错误处理分层：核心库使用自定义错误类型，应用层可使用 anyhow；禁止滥用 unwrap()/expect()，可预见错误场景严禁使用。
- 对外公开的 API 必须有文档注释与示例；unsafe 代码必须有详细的安全性论证。

### 后端通用（Go/Java 等）

- 分层清晰：Controller/Service/Repository 职责严格分离，禁止跨层逻辑。
- 数据库操作禁止拼接 SQL，必须使用参数化查询。
- 外部依赖必须通过接口注入，便于单元测试与替换实现。
- 关键业务路径必须有完整日志、埋点、监控指标。

## 五、重构与防腐化规则（杜绝屎山堆砌）

### 重复代码零容忍

- 同一逻辑出现第二次时必须抽象，出现第三次必须重构。
- 宁可花时间抽取公共方法、公共组件、公共模块，也绝不复制粘贴代码。
- 复制粘贴式开发直接视为不合格代码，必须返工。

### 复杂逻辑必须拆解

- 遇到巨型函数、巨型类、深层嵌套，必须拆分重构，不能在原有屎山上继续叠加。
- 复杂业务逻辑必须拆解为多个小步骤，每个步骤职责单一，组合完成整体功能。

### 坏味道及时治理

- 发现命名混乱、职责模糊、硬编码、重复逻辑等代码坏味道，必须同步治理，不能放任积累。
- 接手遗留代码时，遵循 “童子军规则”：离开时让代码比来时更干净。

### 渐进式重构，拒绝破窗效应

- 大型重构分步骤进行，每一步都可运行、可测试、可回滚，禁止一次性大改全项目。
- 不允许 “先写烂代码以后再优化”，技术债务必须记录并明确偿还计划，不能无限累积。

### 禁止补丁式堆砌

- 修复问题必须从根源解决，禁止用打补丁、加特殊判断的方式掩盖问题。
- 一个问题出现多个同类补丁时，必须从设计层面重构解决，禁止持续叠加分支。

## 六、测试与交付通用标准

### 改动必补测试

- 新增功能必须补充对应单元测试；修复 Bug 必须补充回归测试用例，覆盖该问题场景。
- 核心模块测试覆盖率不得因本次改动下降。
- 涉及接口变更，必须补充接口契约测试。

### 提交前全量自检

- 代码提交前必须通过项目既有检查流水线：静态检查、语法校验、单元测试、构建验证。
- 本地必须完整运行验证通过，不得将问题抛给 CI 流水线。

### 兼容性优先

- 所有改动必须保证向后兼容，老用户升级无需额外配置即可正常运行。
- 涉及数据结构变更，必须提供自动迁移逻辑，禁止让用户手动处理数据。

### 文档与代码同步

- 接口、配置、使用方式变更时，对应文档必须同步更新。
- 复杂逻辑、非直观设计必须补充注释，说明设计意图与边界条件。

## 七、红线禁令（任何项目都绝对禁止）

- 禁止复制粘贴重复代码，宁可抽象也不复制。
- 禁止硬编码魔法数字、魔法字符串、业务规则。
- 禁止静默吞掉错误、忽略异常、掩盖问题。
- 禁止突破分层边界、跨层调用、泄露内部逻辑。
- 禁止随意修改公共接口、破坏向下兼容。
- 禁止遗留注释掉的代码、临时调试代码、无说明的 TODO。
- 禁止为了快速交付，绕过现有架构、走捷径、破坏设计。
- 禁止引入未评估的第三方依赖、使用不安全的代码片段。
- 禁止为了炫技使用晦涩语法、黑魔法，牺牲可维护性。
- 禁止在一次提交中混合功能开发、Bug 修复、重构优化多种类型改动。